<Window x:Class="GERENC_WPF.View.WinPrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GERENC_WPF.View"
        mc:Ignorable="d"
        Title="Principal"
        MinWidth="1024"
        MinHeight="640"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= SIDEBAR ================= -->
        <Border Grid.Column="0"
            Padding="16">

            <StackPanel>

                <!-- Header -->
                <TextBlock Text="Sistema"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,24"/>

                <!-- MENU -->
                <Button  Content="Tela Inicial"/>


                <Button Content = "Gerenciamento de Tarefas" Command="{Binding GerTarefa}"/>
                <Button Content = "Gerenciamento de Chaves" Command="{Binding GerChave}"/>
                <Button Content = "Configurações" Command="{Binding GerConfig}"/>
                <Separator Margin="0,16"/>
                <Button Content = " Sair"/>

            </StackPanel>
        </Border>

        <!-- ================= CONTEÚDO ================= -->
        <Border Grid.Column="1"
 
            Padding="20">

            <ContentControl Content="{Binding CurrentView}"/>
        </Border>

    </Grid>
</Window>
